<template><div class="page"><div class="page-header"><h2>旧机翻新</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="renew_id" label="翻新单号" width="110"/><el-table-column prop="company_id" label="公司" width="80"/><el-table-column label="状态" width="80"><template #default="{row}"><el-tag :type="row.current_status==='3'?'success':'warning'" size="small">{{ row.current_status||'-' }}</el-tag></template></el-table-column><el-table-column prop="firstor" label="负责人" width="80"/><el-table-column label="创建日期" width="100"><template #default="{row}">{{ row.create_time || '-' }}</template></el-table-column><el-table-column label="完成日期" width="100"><template #default="{row}">{{ row.close_time || '-' }}</template></el-table-column></el-table>
    <el-dialog :title="'翻新单 — '+(detail?.renew_id||'')" v-model="drawer" width="500px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="单号">{{ detail.renew_id }}</el-descriptions-item><el-descriptions-item label="公司">{{ detail.company_id||'-' }}</el-descriptions-item><el-descriptions-item label="状态">{{ detail.current_status }}</el-descriptions-item><el-descriptions-item label="负责人">{{ detail.firstor||'-' }}</el-descriptions-item><el-descriptions-item label="创建时间">{{ detail.create_time }}</el-descriptions-item><el-descriptions-item label="完成时间">{{ detail.close_time||'-' }}</el-descriptions-item><el-descriptions-item label="详情" :span="2">{{ detail.detail_description||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchMaintenanceRenovate} from '@/api/itsm';import type {MntRecord} from '@/api/itsm'
const{items,loading,page,perPage,total}=useListPage<MntRecord>(fetchMaintenanceRenovate);const{drawer,detail,open}=useDetailDrawer<MntRecord>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>