<template><div class="page"><div class="page-header"><h2>保养计划</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="id" label="ID" width="60"/><el-table-column prop="plan_y" label="计划年" width="70"/><el-table-column prop="plan_yymm" label="计划年月" width="90"/><el-table-column label="区域" width="80"><template #default="{row}">{{ areaName(row.area_id) }}</template></el-table-column><el-table-column prop="plan_qty" label="计划保养量" width="100" align="center"/><el-table-column label="创建人" width="80"><template #default="{row}">{{ userName(row.creator) }}</template></el-table-column><el-table-column prop="create_time" label="创建日期" width="110"/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'保养计划详情'" v-model="drawer" width="450px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="ID">{{ detail.id }}</el-descriptions-item><el-descriptions-item label="计划年">{{ detail.plan_y }}</el-descriptions-item><el-descriptions-item label="计划年月">{{ detail.plan_yymm }}</el-descriptions-item><el-descriptions-item label="区域">{{ areaName(detail.area_id) }}</el-descriptions-item><el-descriptions-item label="计划保养量">{{ detail.plan_qty||0 }}</el-descriptions-item><el-descriptions-item label="创建人">{{ userName(detail.creator) }}</el-descriptions-item><el-descriptions-item label="创建日期">{{ detail.create_time||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {useUserNames} from '@/composables/useUserNames';import {useArea} from '@/composables/useArea';import {fetchMaintenancePlans} from '@/api/itsm';import type {MntRecord} from '@/api/itsm'
const{items,loading,page,perPage,total}=useListPage<MntRecord>(fetchMaintenancePlans);const{drawer,detail,open}=useDetailDrawer<MntRecord>();const{userName}=useUserNames();const{areaName}=useArea()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
