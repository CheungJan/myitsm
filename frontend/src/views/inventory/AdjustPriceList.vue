<template><div class="page"><div class="page-header"><h2>调价记录</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="pabillid" label="调价单号" width="120"/><el-table-column prop="itemcd" label="物料编码" width="100"/><el-table-column label="原价" width="110" align="right"><template #default="{row}">¥{{ Number(row.oldprice||0).toLocaleString() }}</template></el-table-column><el-table-column label="新价" width="110" align="right"><template #default="{row}">¥{{ Number(row.newprice||0).toLocaleString() }}</template></el-table-column><el-table-column prop="busityp" label="价格类型" width="80"/><el-table-column prop="lineno" label="行号" width="60"/><el-table-column prop="gendate" label="调价日期" width="110"/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'调价详情'" v-model="drawer" width="450px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="调价单号">{{ detail.pabillid||'-' }}</el-descriptions-item><el-descriptions-item label="物料编码">{{ detail.itemcd }}</el-descriptions-item><el-descriptions-item label="原价">¥{{ Number(detail.oldprice||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="新价">¥{{ Number(detail.newprice||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="价格类型">{{ detail.busityp||'-' }}</el-descriptions-item><el-descriptions-item label="行号">{{ detail.lineno }}</el-descriptions-item><el-descriptions-item label="调价日期">{{ detail.gendate||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchAdjustPrices} from '@/api/inventory';import type {InvRecord} from '@/api/inventory'
const{items,loading,page,perPage,total}=useListPage<InvRecord>((p?:Record<string,string>)=>fetchAdjustPrices(p?.pabillid||'',p));const{drawer,detail,open}=useDetailDrawer<InvRecord>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
