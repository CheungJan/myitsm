<template><div class="page"><div class="page-header"><h2>价格管理</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="itemcd" label="物料编码" width="100"/><el-table-column prop="busityp" label="价格类型" width="90"/><el-table-column label="单价" width="110" align="right"><template #default="{row}">¥{{ Number(row.itemprice||0).toLocaleString() }}</template></el-table-column><el-table-column prop="unitcd" label="单位" width="70"/><el-table-column label="当前有效" width="80"><template #default="{row}"><el-tag :type="row.is_current==='1'?'success':'info'" size="small">{{ row.is_current==='1'?'是':'否' }}</el-tag></template></el-table-column><el-table-column prop="effective_date" label="生效日期" width="100"/><el-table-column prop="expire_date" label="失效日期" width="100"/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'价格详情'" v-model="drawer" width="450px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="物料编码">{{ detail.itemcd }}</el-descriptions-item><el-descriptions-item label="价格类型">{{ detail.busityp||'-' }}</el-descriptions-item><el-descriptions-item label="单价">¥{{ Number(detail.itemprice||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="单位">{{ detail.unitcd||'-' }}</el-descriptions-item><el-descriptions-item label="生效日期">{{ detail.effective_date||'-' }}</el-descriptions-item><el-descriptions-item label="失效日期">{{ detail.expire_date||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchPrices} from '@/api/inventory';import type {InvRecord} from '@/api/inventory'
const{items,loading,page,perPage,total}=useListPage<InvRecord>(fetchPrices);const{drawer,detail,open}=useDetailDrawer<InvRecord>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
