<template><div class="page"><div class="page-header"><h2>库存预警规则</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="itemcd" label="物料编码" width="100"/><el-table-column label="库存下限" width="90" align="right"><template #default="{row}">{{ row.invlow||'-' }}</template></el-table-column><el-table-column label="库存上限" width="90" align="right"><template #default="{row}">{{ row.invhigh||'-' }}</template></el-table-column><el-table-column label="天数下限" width="90" align="right"><template #default="{row}">{{ row.daylow||'-' }}</template></el-table-column><el-table-column label="天数上限" width="90" align="right"><template #default="{row}">{{ row.dayhigh||'-' }}</template></el-table-column><el-table-column label="有效" width="60"><template #default="{row}"><el-tag :type="row.useflg==='1'?'success':'info'" size="small">{{ row.useflg==='1'?'是':'否' }}</el-tag></template></el-table-column></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'预警规则 — '+(detail?.itemcd||'')" v-model="drawer" width="450px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="物料编码">{{ detail.itemcd }}</el-descriptions-item><el-descriptions-item label="库存下限">{{ detail.invlow||'-' }}</el-descriptions-item><el-descriptions-item label="库存上限">{{ detail.invhigh||'-' }}</el-descriptions-item><el-descriptions-item label="天数下限">{{ detail.daylow||'-' }}</el-descriptions-item><el-descriptions-item label="天数上限">{{ detail.dayhigh||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchInventoryLimits} from '@/api/inventory';import type {InvRecord} from '@/api/inventory'
const{items,loading,page,perPage,total}=useListPage<InvRecord>(fetchInventoryLimits);const{drawer,detail,open}=useDetailDrawer<InvRecord>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
