<template><div class="page"><div class="page-header"><h2>押金出入流水</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column label="磁卡号" width="100"><template #default="{row}">{{ row.cust_card || custCard(row.custcd) }}</template></el-table-column><el-table-column label="原金额" width="110" align="right"><template #default="{row}">¥{{ Number(row.old_a||0).toLocaleString() }}</template></el-table-column><el-table-column label="新金额" width="110" align="right"><template #default="{row}">¥{{ Number(row.new_a||0).toLocaleString() }}</template></el-table-column><el-table-column label="变更金额" width="110" align="right"><template #default="{row}">¥{{ Number(row.change_a||0).toLocaleString() }}</template></el-table-column><el-table-column prop="r_billid" label="关联单号" width="100"/><el-table-column prop="update_time" label="更新时间" width="110"/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'流水详情'" v-model="drawer" width="400px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="磁卡号">{{ detail.cust_card || custCard(detail.custcd) }}</el-descriptions-item><el-descriptions-item label="原金额">¥{{ Number(detail.old_a||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="新金额">¥{{ Number(detail.new_a||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="变更金额">¥{{ Number(detail.change_a||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="关联单号">{{ detail.r_billid||'-' }}</el-descriptions-item><el-descriptions-item label="更新时间">{{ detail.update_time||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {useCustomerCards} from '@/composables/useCustomerCards';import {fetchDepositIO} from '@/api/deposit';import type {DepositRecord} from '@/api/deposit'
const{items,loading,page,perPage,total}=useListPage<DepositRecord>(fetchDepositIO);const{drawer,detail,open}=useDetailDrawer<DepositRecord>();const{custCard}=useCustomerCards()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
