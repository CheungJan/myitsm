<template><div class="page"><div class="page-header"><h2>资产属性扩展</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="eid" label="设备SN" width="130"/><el-table-column prop="item_nm" label="物料名称" min-width="120"/><el-table-column label="资产类型" width="90"><template #default="{row}">{{ row.asset_type||'-' }}</template></el-table-column><el-table-column label="所属方" width="80"><template #default="{row}">{{ row.asset_owner||'-' }}</template></el-table-column><el-table-column label="设备状态" width="80"><template #default="{row}">{{ row.sflg||'-' }}</template></el-table-column><el-table-column label="门店状态" width="80"><template #default="{row}">{{ row.asset_status||'-' }}</template></el-table-column><el-table-column label="回收状态" width="80"><template #default="{row}">{{ row.recycle_status||'-' }}</template></el-table-column><el-table-column prop="install_date" label="安装日期" width="100"/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'资产属性 — '+(detail?.eid||'')" v-model="drawer" width="550px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="设备SN">{{ detail.eid }}</el-descriptions-item><el-descriptions-item label="物料名称">{{ detail.item_nm||'-' }}</el-descriptions-item><el-descriptions-item label="资产类型">{{ detail.asset_type||'-' }}</el-descriptions-item><el-descriptions-item label="所属方">{{ detail.asset_owner||'-' }}</el-descriptions-item><el-descriptions-item label="设备状态">{{ detail.sflg||'-' }}</el-descriptions-item><el-descriptions-item label="门店状态">{{ detail.asset_status||'-' }}</el-descriptions-item><el-descriptions-item label="回收状态">{{ detail.recycle_status||'-' }}</el-descriptions-item><el-descriptions-item label="安装日期">{{ detail.install_date||'-' }}</el-descriptions-item><el-descriptions-item label="生产日期">{{ detail.prddate||'-' }}</el-descriptions-item><el-descriptions-item label="所属整机">{{ detail.host_eid||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchAssets} from '@/api/master'
const{items,loading,page,perPage,total}=useListPage<Record<string,unknown>>((p?:Record<string,string>)=>fetchAssets(p||{})as any);const{drawer,detail,open}=useDetailDrawer<Record<string,unknown>>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
