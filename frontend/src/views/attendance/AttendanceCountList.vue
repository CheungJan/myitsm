<template><div class="page"><div class="page-header"><h2>考勤汇总</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="operid" label="员工ID" width="90"/><el-table-column prop="opernm" label="姓名" width="100"/><el-table-column prop="amonth" label="月份" width="90"/><el-table-column label="迟到次数" width="80" align="center"><template #default="{row}">{{ row.latecount||0 }}</template></el-table-column><el-table-column label="早退次数" width="80" align="center"><template #default="{row}">{{ row.leavecount||0 }}</template></el-table-column><el-table-column label="缺勤次数" width="80" align="center"><template #default="{row}">{{ row.absentcount||0 }}</template></el-table-column><el-table-column prop="memo" label="备注" min-width="120" show-overflow-tooltip/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'考勤汇总 — '+(detail?.opernm||'')" v-model="drawer" width="500px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="员工ID">{{ detail.operid }}</el-descriptions-item><el-descriptions-item label="姓名">{{ detail.opernm||'-' }}</el-descriptions-item><el-descriptions-item label="月份">{{ detail.amonth }}</el-descriptions-item><el-descriptions-item label="迟到次数">{{ detail.latecount||0 }}</el-descriptions-item><el-descriptions-item label="早退次数">{{ detail.leavecount||0 }}</el-descriptions-item><el-descriptions-item label="缺勤次数">{{ detail.absentcount||0 }}</el-descriptions-item><el-descriptions-item label="备注" :span="2">{{ detail.memo||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchAttendanceSummary} from '@/api/attendance';import type {AttendRecord} from '@/api/attendance'
const{items,loading,page,perPage,total}=useListPage<AttendRecord>(fetchAttendanceSummary);const{drawer,detail,open}=useDetailDrawer<AttendRecord>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
