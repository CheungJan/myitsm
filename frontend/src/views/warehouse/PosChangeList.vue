<template><div class="page"><div class="page-header"><h2>设备回收确认</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column label="记录ID" width="80"><template #default="{row}">{{ row.id }}</template></el-table-column><el-table-column prop="poseid" label="POS设备ID" width="140"/><el-table-column prop="remark" label="备注" min-width="180" show-overflow-tooltip/><el-table-column label="更新日期" width="100"><template #default="{row}">{{ row.upddate || row.gendate || '-' }}</template></el-table-column><el-table-column prop="opercd" label="操作员" width="80"/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card>
    <el-dialog :title="'设备回收 — '+(detail?.poseid||'')" v-model="drawer" width="500px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="POS设备ID">{{ detail.poseid }}</el-descriptions-item><el-descriptions-item label="操作员">{{ detail.opercd }}</el-descriptions-item><el-descriptions-item label="更新日期">{{ detail.upddate }}</el-descriptions-item><el-descriptions-item label="有效"><el-tag :type="detail.useflg==='1'?'success':'danger'" size="small">{{ detail.useflg==='1'?'有效':'无效' }}</el-tag></el-descriptions-item><el-descriptions-item label="备注" :span="2">{{ detail.remark || '-' }}</el-descriptions-item></el-descriptions></template></el-dialog>
  </div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import request from '@/api/request'
interface PC{id:number;poseid:string;opercd:string;upddate:string;useflg:string;remark:string;[key:string]:unknown}
const{items,loading,page,perPage,total}=useListPage<PC>((p)=>request.get('/warehouse/pos-change',{params:p}));const{drawer,detail,open}=useDetailDrawer<PC>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>