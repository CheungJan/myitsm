<template><div class="page"><div class="page-header"><h2>应收管理</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="ar_id" label="应收编号" width="100"/><el-table-column prop="custcd" label="客户" width="100"/><el-table-column label="应收金额" width="110" align="right"><template #default="{row}">¥{{ Number(row.amount||0).toLocaleString() }}</template></el-table-column><el-table-column label="已收金额" width="110" align="right"><template #default="{row}">¥{{ Number(row.paid_amount||0).toLocaleString() }}</template></el-table-column><el-table-column label="余额" width="110" align="right"><template #default="{row}">¥{{ Number(row.balance||0).toLocaleString() }}</template></el-table-column><el-table-column label="状态" width="80"><template #default="{row}"><el-tag :type="arStatusTag(row.status)" size="small">{{ row.status||'PENDING' }}</el-tag></template></el-table-column><el-table-column prop="ar_date" label="应收日期" width="100"/><el-table-column prop="due_date" label="到期日" width="100"/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'应收 — '+(detail?.ar_id||'')" v-model="drawer" width="550px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="应收编号">{{ detail.ar_id }}</el-descriptions-item><el-descriptions-item label="客户">{{ detail.custcd||'-' }}</el-descriptions-item><el-descriptions-item label="科目编码">{{ detail.account_cd||'-' }}</el-descriptions-item><el-descriptions-item label="关联账单">{{ detail.bill_id||'-' }}</el-descriptions-item><el-descriptions-item label="关联发票">{{ detail.fpbh||'-' }}</el-descriptions-item><el-descriptions-item label="关联合同">{{ detail.htbh||'-' }}</el-descriptions-item><el-descriptions-item label="应收金额">¥{{ Number(detail.amount||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="已收金额">¥{{ Number(detail.paid_amount||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="余额">¥{{ Number(detail.balance||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="状态"><el-tag :type="arStatusTag(detail.status)" size="small">{{ detail.status||'PENDING' }}</el-tag></el-descriptions-item><el-descriptions-item label="应收日期">{{ detail.ar_date||'-' }}</el-descriptions-item><el-descriptions-item label="到期日">{{ detail.due_date||'-' }}</el-descriptions-item><el-descriptions-item label="备注" :span="2">{{ detail.remark||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchReceivables} from '@/api/finance';import type {FinanceRecord} from '@/api/finance'
const{items,loading,page,perPage,total}=useListPage<FinanceRecord>(fetchReceivables);const{drawer,detail,open}=useDetailDrawer<FinanceRecord>()
function arStatusTag(s:string){const m:Record<string,string>={PENDING:'info',PARTIAL:'warning',PAID:'success',OVERDUE:'danger',CANCELLED:'info'};return m[s]||'info'}</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
