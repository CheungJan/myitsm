<template><div class="page"><div class="page-header"><h2>设备折旧</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="eid" label="设备SN" width="130"/><el-table-column prop="item_cd" label="物料编码" width="100"/><el-table-column label="原值" width="110" align="right"><template #default="{row}">¥{{ Number(row.original_value||0).toLocaleString() }}</template></el-table-column><el-table-column label="残值" width="90" align="right"><template #default="{row}">¥{{ Number(row.salvage_value||0).toLocaleString() }}</template></el-table-column><el-table-column label="月折旧额" width="100" align="right"><template #default="{row}">¥{{ Number(row.monthly_amount||0).toLocaleString() }}</template></el-table-column><el-table-column label="累计折旧" width="110" align="right"><template #default="{row}">¥{{ Number(row.accumulated||0).toLocaleString() }}</template></el-table-column><el-table-column label="净值" width="110" align="right"><template #default="{row}">¥{{ Number(row.net_value||0).toLocaleString() }}</template></el-table-column><el-table-column label="折旧方法" width="80"><template #default="{row}">{{ row.method||'SL' }}</template></el-table-column><el-table-column prop="start_date" label="起始日" width="100"/></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'折旧 — '+(detail?.eid||'')" v-model="drawer" width="550px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="设备SN">{{ detail.eid }}</el-descriptions-item><el-descriptions-item label="物料编码">{{ detail.item_cd||'-' }}</el-descriptions-item><el-descriptions-item label="原值">¥{{ Number(detail.original_value||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="残值">¥{{ Number(detail.salvage_value||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="使用年限">{{ detail.useful_life_months||'-' }}个月</el-descriptions-item><el-descriptions-item label="折旧方法">{{ detail.method||'SL' }}</el-descriptions-item><el-descriptions-item label="月折旧额">¥{{ Number(detail.monthly_amount||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="累计折旧">¥{{ Number(detail.accumulated||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="净值">¥{{ Number(detail.net_value||0).toLocaleString() }}</el-descriptions-item><el-descriptions-item label="起始日">{{ detail.start_date||'-' }}</el-descriptions-item><el-descriptions-item label="最后计算日">{{ detail.last_calc_date||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchDepreciations} from '@/api/finance';import type {FinanceRecord} from '@/api/finance'
const{items,loading,page,perPage,total}=useListPage<FinanceRecord>(fetchDepreciations);const{drawer,detail,open}=useDetailDrawer<FinanceRecord>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
