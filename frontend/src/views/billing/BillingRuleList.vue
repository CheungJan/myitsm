<template><div class="page"><div class="page-header"><h2>结算规则</h2></div><el-card shadow="never"><el-table :data="items" v-loading="loading" stripe size="small" highlight-current-row @row-click="open"><el-table-column prop="rule_id" label="规则编号" width="100"/><el-table-column prop="rule_name" label="规则名称" min-width="150"/><el-table-column label="结算类型" width="80"><template #default="{row}"><el-tag size="small">{{ row.billing_type||'-' }}</el-tag></template></el-table-column><el-table-column label="结算周期" width="90"><template #default="{row}">{{ row.cycle_type||'MONTHLY' }}</template></el-table-column><el-table-column label="价格来源" width="80"><template #default="{row}">{{ row.price_source||'-' }}</template></el-table-column><el-table-column label="税率" width="70"><template #default="{row}">{{ row.tax_rate||0 }}%</template></el-table-column><el-table-column label="滞纳金率" width="80"><template #default="{row}">{{ row.late_fee_rate||0 }}%/天</template></el-table-column><el-table-column label="有效" width="60"><template #default="{row}"><el-tag :type="row.useflg==='1'?'success':'info'" size="small">{{ row.useflg==='1'?'是':'否' }}</el-tag></template></el-table-column></el-table><AppPagination v-model:current-page="page" v-model:page-size="perPage" :total="total" style="margin-top:12px;justify-content:flex-end"/></el-card><el-dialog :title="'规则 — '+(detail?.rule_name||'')" v-model="drawer" width="500px"><template v-if="detail"><el-descriptions :column="2" border size="small"><el-descriptions-item label="规则编号">{{ detail.rule_id }}</el-descriptions-item><el-descriptions-item label="规则名称">{{ detail.rule_name }}</el-descriptions-item><el-descriptions-item label="结算类型">{{ detail.billing_type }}</el-descriptions-item><el-descriptions-item label="结算周期">{{ detail.cycle_type||'MONTHLY' }}</el-descriptions-item><el-descriptions-item label="价格来源">{{ detail.price_source||'MODEL' }}</el-descriptions-item><el-descriptions-item label="税率">{{ detail.tax_rate||0 }}%</el-descriptions-item><el-descriptions-item label="滞纳金日费率">{{ detail.late_fee_rate||0 }}</el-descriptions-item><el-descriptions-item label="备注" :span="2">{{ detail.remark||'-' }}</el-descriptions-item></el-descriptions></template></el-dialog></div></template>
<script setup lang="ts">import AppPagination from '@/components/common/AppPagination.vue';import {useListPage} from '@/composables/useListPage';import {useDetailDrawer} from '@/composables/useDetailDrawer';import {fetchBillingRules} from '@/api/billing';import type {BillingRecord} from '@/api/billing'
const{items,loading,page,perPage,total}=useListPage<BillingRecord>(fetchBillingRules);const{drawer,detail,open}=useDetailDrawer<BillingRecord>()</script>
<style scoped>.page{padding:0}.page-header{display:flex;justify-content:space-between;margin-bottom:16px}.page-header h2{font-size:18px;font-weight:600;margin:0}</style>
