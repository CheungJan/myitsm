# 数据字典治理摘要（一页版）

## 1. 口径与输入
- 主产物：`数据库实体与状态码字典_优化后_最终版_utf8bom.csv`
- 输入来源：代码SQL抽取 + 旧版字典 + 实库对象清单（CCGL/CCGLlsgl）
- 说明：`TIT01_TIMEPOINT_CUST` 已在本地测试库 `CCGL_TEST` 完成兼容视图创建与验证，正式库待DBA窗口执行。

## 2. 分类结果
- 保留：130
- 待迁移：28
- 待下线：12

## 3. 为什么删 / 为什么留
- 保留：代码命中且CCGL在用，属于当前业务主路径对象。
- 待迁移：代码在用但实库缺失（含缺失视图），需先兼容层再迁移或改SQL。
- 待下线：仅旧字典存在、代码未命中且双库不存在，建议纳入下线评审池。

## 4. 下一步执行顺序
1. 先执行P1：将 `TIT01_TIMEPOINT_CUST` 在正式CCGL按已验证脚本落地，并做只读验证。
2. 再执行P2：按`缺失对象治理清单_更新.md`分批做同义词/视图兼容，逐项回归。
3. 对“待下线”对象先做30天访问审计（SQL/报表/作业），无命中再归档下线。
4. 周度回写该字典，保持“治理标签”与执行状态一致。

## 5. 风险控制
- 全程先测试后正式；正式执行需会话校验 `SERVICE_NAME`。
- 仅使用视图/同义词兼容，不做删表/改表结构的破坏性动作。
