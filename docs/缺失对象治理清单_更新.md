# 缺失对象治理清单（更新后）

- 总待处理：25（已剔除实库已存在对象）
- P1：1；P2：24

## P1（先处理）
- `TIT01_TIMEPOINT_CUST` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL

## P2（第二批）
- `TAC01_ACRECEIVE` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TAC11_PAYDAYS` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TAC12_CREDLIMIT` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TAC13_CREDLIMITDT` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TIP05_CUSTDISCOUNT` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TIP07_COSTPRICTRACK` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TIP08_MONTHCOST` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TIV30_SHOPCHECKPLAN` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TIV33_OVBILL` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TMM13_ITEMUNIT` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TMM14_ITEMBAR` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TMM15_ITEMTAXRATE` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TMM23_ADDRESS` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TPC01_PCPROMPLAN` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TPS01_PIPELINE` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TSL05_SLPROMPLAN` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TSL11_REGISTER` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TSL11_SLBILLITEM` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TSL15_PICKBILL` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TSL16_PICKBILLDT` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TSL20_INVOICE` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TWH03_ROUTE` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `TWH22_ARRTOROUTEDT` | 对象缺失 | 优先建同义词过渡；确认后再做物理迁移/改SQL
- `U_BUTTONS_V` | 视图缺失 | 优先在CCGL创建兼容视图或同义词（指向现有报表视图）