# PB源码SQL提取分析（2026-04-14）

## 分析目的
对比PB源码中的实际SQL与之前基于数据库字典的重构实现，识别差异和需要调整的地方。

## 文件识别方法

**严谨的识别方法**：通过 Grep 搜索文件内容中是否包含表名（如 `TIT15`、`EQUIPMENT_RENOVATE`），而非仅基于文件名。

**TIT15 相关文件清单（9个，通过 Grep 搜索 `TIT15` 识别）**：
- `itsm.pbl\w_r_itsm_renovate.srw` - 翻新单窗口
- `itsm.pbl\w_r_itsm_d2d.srw` - 上门服务窗口
- `itsm.pbl\u_itsm_renovate.sru` - 翻新单 UserObject
- `itsm.pbl\d_renovat_report.srd` - 翻新报表
- `itsm.pbl\d_maintenance_renovate_one.srd` - 翻新单详情
- `itsm.pbl\d_maintenance_renovate_list.srd` - 翻新单列表
- `itsm.pbl\d_maintenance_renovate_free.srd` - 翻新设备查询
- `itsm.pbl\d_itsm_renovate_list.srd` - 设备列表
- `itsm.pbl\d_itsm_renovate_free.srd` - 设备查询

**说明**：SQL 主要存在于 UserObject (.sru) 文件中，DataWindow (.srd) 主要包含 UI 布局和列定义。

---

## 1. TIT15_EQUIPMENT_RENOVATE（旧机翻新设备明细）

### PB源码SQL（来源：itsm.pbl\u_itsm_renovate.sru）

```sql
Select count(*) Into :ll_count From TIT15_EQUIPMENT_RENOVATE
```

**上下文**：在翻新单关闭逻辑中，检查是否存在翻新设备明细记录。

### 现有实现（S044）

| 方法 | SQL | 对比 |
|------|-----|------|
| `list_equipment_renovates` | `SELECT * FROM TIT15_EQUIPMENT_RENOVATE WHERE RENOVATE_ID=:renovate_id` | ✅ 符合业务需求 |
| `create_equipment_renovate` | `INSERT INTO TIT15_EQUIPMENT_RENOVATE (...)` | ✅ 符合业务需求 |

**结论**：现有实现与PB源码逻辑一致，无需调整。

---

## 2. TIT17_CUST_POS_DAILY（保养设备明细）

### PB源码SQL（来源：itsm02.pbl\u_itsm_maintenance_daily.sru）

```sql
//INSERT INTO TIT17_CUST_POS_DAILY
//      SELECT :is_mid,:is_custcd,eid,itemcd,startdate,sysinfo,softinfo,
//posupddate,posinfo,area,'1','1',
//INSERT INTO TIT17_CUST_POS_DAILY
//      SELECT :is_mid,:is_custcd,eid,itemcd,startdate,sysinfo,softinfo,
//posupddate,posinfo,area,'1','2',
INSERT INTO TIT17_CUST_POS_DAILY
                        UPDATE_TIME          ,
```

**上下文**：在保养单保存逻辑中，插入保养设备明细。注意代码中有注释掉的INSERT语句，实际执行的是UPDATE_TIME更新。

### 现有实现（S045）

| 方法 | SQL | 对比 |
|------|-----|------|
| `list_cust_pos_daily` | `SELECT * FROM TIT17_CUST_POS_DAILY WHERE DAILY_MAINTENANCE_ID=:daily_maintenance_id` | ✅ 符合业务需求 |
| `create_cust_pos_daily` | `INSERT INTO TIT17_CUST_POS_DAILY (...)` | ⚠️ 需确认字段映射 |

**差异分析**：
- PB源码中INSERT的字段：`DAILY_MAINTENANCE_ID(:is_mid), CUST_CD(:is_custcd), EID, ITEMCD, STARTDATE, SYSINFO, SOFTINFO, POSUPDATE_DATE, POSINFO, AREA, '1', '1'/'1','2'`
- 现有实现字段：`daily_maintenance_id, business_operation_id, cust_cd, eid, item_cd, typflg, creator`

**需要确认**：
1. `typflg` 字段是否对应 PB 中的 `'1'/'1','2'` 逻辑
2. 是否缺少 `STARTDATE, SYSINFO, SOFTINFO, POSUPDATE_DATE, POSINFO, AREA` 字段

---

## 3. TIT23_MAINTENANCE_D2D（上门服务记录）

### PB源码SQL（来源：itsm.pbl\u_itsm_renovate.sru）

```sql
Select count(*) Into :ll_count From tit24_maintenance_rv Where maintenance_id = :is_mid
```

**上下文**：在翻新单中检查回访记录（TIT24），非TIT23。

### 现有实现（S046）

| 方法 | SQL | 对比 |
|------|-----|------|
| `list_d2d_records` | `SELECT * FROM TIT23_MAINTENANCE_D2D WHERE MAINTENANCE_ID=:maintenance_id` | ✅ 符合业务需求 |
| `create_d2d_record` | `INSERT INTO TIT23_MAINTENANCE_D2D (...)` | ✅ 符合业务需求 |

**结论**：现有实现与PB源码逻辑一致，无需调整。

---

## 4. TIT28_FREE_REPLACE（免费更换主表）- **M005 Phase 1**

### PB源码SQL（来源：itsm02.pbl\u_itsm_free_replace.sru / u_itsm_store_close.sru）

```sql
select count(*) into :ll_count from TIT28_FREE_REPLACE_DT
SELECT count(*) INTO :ll_count FROM TIT28_FREE_REPLACE_DT
```

**上下文**：在免费更换单和门店关闭单中检查免费更换设备明细记录。

**结论**：TIT28_FREE_REPLACE 是M005 Phase 1目标单据，尚未实现，无对比。

---

## 5. TIT27_CLOSE_BILLS（关单表）

### PB源码SQL（来源：itsm.pbl\u_itsm_renovate.sru / itsm02.pbl\u_itsm_maintenance_daily.sru / u_itsm_free_replace.sru）

```sql
Insert Into TIT27_CLOSE_BILLS
```

**上下文**：在翻新单关闭、保养单关闭、免费更换关闭时插入关单记录。

**结论**：TIT27_CLOSE_BILLS 是公用关单表，在多个单据关闭时使用，尚未实现，无对比。

---

## 6. TIT24_MAINTENANCE_RV（回访记录）

### PB源码SQL（来源：itsm.pbl\u_itsm_renovate.sru / itsm02.pbl\u_itsm_maintenance_daily.sru / u_itsm_free_replace.sru）

```sql
Select count(*) Into :ll_count From tit24_maintenance_rv Where maintenance_id = :is_mid
SELECT count(*) INTO :ll_count FROM tit24_maintenance_rv Where maintenance_id = :is_mid
```

**上下文**：在翻新单、保养单、免费更换单中检查回访记录。

**结论**：TIT24_MAINTENANCE_RV 是回访记录表，属于公用附表，尚未实现，无对比。

---

## 总结与调整建议

### ✅ 无需调整
- **TIT15_EQUIPMENT_RENOVATE**（S044）：现有实现与PB源码逻辑一致
- **TIT23_MAINTENANCE_D2D**（S046）：现有实现与PB源码逻辑一致

### ⚠️ 需要确认/调整
- **TIT17_CUST_POS_DAILY**（S045）：
  - 需确认字段映射是否完整（特别是 STARTDATE, SYSINFO, SOFTINFO, POSUPDATE_DATE, POSINFO, AREA）
  - 需确认 `typflg` 字段的业务含义

### 📋 M005 Phase 1 待实现
- **TIT18_STORE_CLOSE**：门店关闭单（独立单据）
- **TIT28_FREE_REPLACE / TIT28_FREE_REPLACE_DT**：免费更换主子表
- **TIT27_CLOSE_BILLS**：关单表（公用）
- **TIT24_MAINTENANCE_RV**：回访记录表（公用）

---

## 下一步行动

1. **优先级1**：确认 TIT17_CUST_POS_DAILY 字段映射是否需要调整
2. **优先级2**：启动 M005 Phase 1 实现（TIT18_STORE_CLOSE / TIT28_FREE_REPLACE）
3. **优先级3**：考虑实现公用表（TIT27_CLOSE_BILLS / TIT24_MAINTENANCE_RV）
