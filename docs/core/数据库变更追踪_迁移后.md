# 数据库变更追踪（迁移后 → P0 完成）

> 记录 2026-05-08 迁移完成至 2026-05-27 之间所有表结构/字段/索引变更。
> 基准文档：`数据迁移执行方案_v2.md`（迁移时）/ `数据库字典_精简后_最终版.md`（Oracle 基线）
> 当前状态：`数据库字典_PostgreSQL当前版.md`（143 表）+ `数据库ER关系文档.md`（144 模型）

---

## 一、新增表（12 张）

迁移时 130 张表 → P0 完成 142 张表。新增来源：

| 表名 | 来源 | 说明 |
|------|------|------|
| `tmm02_country` | Oracle 遗留→已建模 | 国家字典（192条），客户管理四级联动 |
| `tmm03_province` | Oracle 遗留→已建模 | 省份字典（34条） |
| `tmm04_city` | Oracle 遗留→已建模 | 城市字典（436条） |
| `tmm05_town` | Oracle 遗留→已建模 | 区县字典（2,778条） |
| `tmm46_area` | Oracle 等价迁移 | 区域（20条），字段补全 |
| ~~`tmm47_commode`~~ | 2026-07-13 drop | 数据迁移至 `tmm31_syscodes`（code_typ='CM'） |
| `tmm22_customers_history` | P0 优化新增 | 客户磁卡号变更历史 |
| `tmm62_asset_attrib_list` | 4.3 业务必须表 | 资产属性清单 |
| `tiv11_detail` | 4.3 业务必须表 | 库存明细 |
| `tiv12_detaildt` | 4.3 业务必须表 | 库存明细流水 |
| `ttx01_txkmg` | 4.3 业务必须表 | 调拨科目管理 |
| `tmp14_checkindt` | 4.3 业务必须表 | 采购验收明细 |

---

## 二、字段新增

### 2.1 TMM43_EID（+5 字段，P0）

| 字段 | 类型 | 说明 |
|------|------|------|
| `asset_type` | VARCHAR(10) | 资产类型（AT: 01新机/02旧机/03翻新机/04报废） |
| `recyclable` | BOOLEAN | 可回收标志 |
| `recycle_status` | VARCHAR(10) | 回收状态（RS） |
| `asset_owner` | VARCHAR(10) | 资产所属方（OW: 01公司/02代理商/03客户） |
| `install_date` | TIMESTAMP | 安装日期 |

> 来源：从 CustPosRl 迁入，设备维度资产管理。

### 2.2 TMM43_EID_TRACK（+12 字段，P0）

| 字段 | 类型 | 说明 |
|------|------|------|
| `install_date` | TIMESTAMP | 安装日期（变更前） |
| `n_install_date` | TIMESTAMP | 安装日期（变更后） |
| `cust_cd` | VARCHAR(8) | 客户编码（变更前） |
| `n_cust_cd` | VARCHAR(8) | 客户编码（变更后） |
| `asset_type` | VARCHAR(10) | 资产类型（变更前） |
| `n_asset_type` | VARCHAR(10) | 资产类型（变更后） |
| `recyclable` | VARCHAR(1) | 可回收标志（变更前） |
| `n_recyclable` | VARCHAR(1) | 可回收标志（变更后） |
| `recycle_status` | VARCHAR(10) | 回收状态（变更前） |
| `n_recycle_status` | VARCHAR(10) | 回收状态（变更后） |
| `asset_owner` | VARCHAR(10) | 资产所属方（变更前） |
| `n_asset_owner` | VARCHAR(10) | 资产所属方（变更后） |

### 2.3 TMM35_CUST_POS_RL（-4 字段，P0）

| 字段 | 操作 | 说明 |
|------|------|------|
| `asset_type` | 删除 | 迁至 TMM43_EID |
| `recyclable` | 删除 | 迁至 TMM43_EID |
| `recycle_status` | 删除 | 迁至 TMM43_EID |
| `install_date` | 删除 | 迁至 TMM43_EID |

### 2.4 TMM44_POS_R_EID（-1 字段）

| 字段 | 操作 | 说明 |
|------|------|------|
| `muitem` | 删除 | 模型从不使用，遗留列 |

### 2.5 twh01_warehouse（+1 字段，2026-07-20）

| 字段 | 类型 | 说明 |
|------|------|------|
| `area_id` | INTEGER | 仓库所属划区（FK→tmm46_area.id），所有仓库通用：物理仓=所在位置，工程师虚拟仓=所属区域 |

### 2.6 tmc13_users（+1 字段，2026-07-20）

| 字段 | 类型 | 说明 |
|------|------|------|
| `default_whcd` | VARCHAR(2) | 用户默认仓库编码（FK→twh01_warehouse.whcd），工程师虚拟仓场景：通过用户直接定位其虚拟仓 |

### 2.7 tit23_maintenance_d2d（+8 字段，2026-07-20 待实施）

| 字段 | 类型 | 说明 |
|------|------|------|
| `gzdm` | VARCHAR(8) | 故障代码 8 位 |
| `device_id` | VARCHAR(13) | 本次处理设备 |
| `accessories_id` | VARCHAR(13) | 本次处理配件 |
| `d2d_phenomenon` | VARCHAR(200) | 实际现象 |
| `d2d_reason` | VARCHAR(200) | 原因 |
| `d2d_handling` | VARCHAR(500) | 处理过程 |
| `d2d_result` | VARCHAR(1) | 结果枚举：1已解决/2未解决/3转修/4待配件/5客户拒修/6非设备问题 |
| `d2d_note` | VARCHAR(200) | 其他补充 |

### 2.8 tit25_accessories_update（+4 字段，2026-07-20 待实施）

| 字段 | 类型 | 说明 |
|------|------|------|
| `paytype` | VARCHAR(30) | 收费类型（吸收 TIT26） |
| `payje` | NUMERIC(10,3) | 收款金额（吸收 TIT26） |
| `paydate` | TIMESTAMP | 收款日期（吸收 TIT26） |
| `memo` | VARCHAR(250) | 备注（吸收 TIT26） |

---

## 三、新增索引（P0，5 个）

| 索引名 | 表 | 列 | 用途 | 迁移 |
|--------|-----|-----|------|------|
| `idx_pos_r_eid_eid` | `tmm44_pos_r_eid` | `eid` | BOM 配件归属解析 | `3b35c25a5736` |
| `idx_pos_r_eid_useflg` | `tmm44_pos_r_eid` | `useflg, eid` | 位置筛选 + useflg 过滤 | `3b35c25a5736` |
| `idx_cust_pos_rl_eid_useflg` | `tmm35_cust_pos_rl` | `eid, useflg` | BOM 父设备客户关系 | `3b35c25a5736` |
| `idx_eid_track_eid_itemcd` | `tmm43_eid_track` | `eid, itemcd` | 历史详情（47ms→0.4ms） | `0d573d25785f` |
| `idx_eid_track_type_eid` | `tmm43_eid_track` | `type, eid` | plan_refid + C 记录查询 | `0d573d25785f` |

---

## 四、新增/启用表（P0-物料增强）

| 表 | 说明 | 状态 |
|----|------|------|
| `tip02_supplier_price` | 供应商价格表（新建，itemcd/supp_cd/min_qty/itemprice/时效） | ✅ 已建表+模型，F2 实现写入 API |
| `tmm24_custitems` | 物料-供应商关联表（迁移时已存在 421 条，P0 启用了 CRUD API） | ✅ API 已就绪（GET/POST/PUT/DELETE） |

## 五、tip01_price 扩展字段（P0-物料增强）

| 字段 | 类型 | 说明 |
|------|------|------|
| `effective_date` | DATE | 生效日期 |
| `expire_date` | DATE | 失效日期 |
| `is_current` | BOOLEAN | 是否当前有效 |

## 六、tmm11_itemclass 字段恢复（P0-物料增强）

| 字段 | 类型 | 说明 |
|------|------|------|
| `opercd` | VARCHAR(6) | 操作员（从 ortopbitsmdb 回填 186 行 TRIM 空格） |
| `gendate` | TIMESTAMP | 创建日期 |
| `upddate` | TIMESTAMP | 更新日期 |

## 七、数据治理操作（详见 P0数据治理记录.md）

详见 `P0数据治理记录.md`：
- 1,230 台孤儿报废（`sflg 1→2`）
- 1,650 台门店退回同步（`sflg 1→0`）
- EID 空格清理（1+2+80 条）
- C 记录日期/单号纠正（单条 + 全局自动纠正机制）

---

## 六、采购结算/退货模块（2026-05-26）

### 新增表

| 表名 | 说明 |
|------|------|
| `tpc14_pcbilldt` | 采购结算明细表（14列），FK→tpc14_pcbill CASCADE，FK→tpc13_registerdt RESTRICT |

### 字段变更

| 表 | 变更 | 详情 |
|----|------|------|
| `tpc14_pcbill` | 重命名 | custcd→suppliercd |
| | 新增 | ref_rgstbillid, pay_type, invoice_no, invoice_date, total_settle_amt, auditflg, auditman, auditdate |
| | 删除 | refbillid（被ref_rgstbillid替代）, pcamt（被total_settle_amt替代） |
| `tpc16_rpcbill` | 重命名 | custcd→suppliercd |
| | 新增 | return_reason |
| `tpc17_rpcbilldt` | 新增 | return_price, return_amt, line_reason |
| `tpc13_registerdt` | 新增约束 | UNIQUE(rgstbillid, lineno) — TPC14_DT 外键前置条件 |

### 编号规则

| 单据 | 前缀 | IdMaster key |
|------|------|-------------|
| 采购结算单 | SB | `SB` |
| 采购退货单 | RT | `RT` |

---

## 八、2026-06-08 列宽修正（F2 业务链验收）

> 背景：`feature/f2-business-chain` 分支验收发现多表列宽不足以容纳新编号规则。  
> 迁移：`0f1a2b3c4d5e` (fix: column lengths)

### 字段变更

| 表 | 列 | 原类型 | 新类型 | 原因 |
|----|-----|--------|--------|------|
| `tms01_work_order` | status | VARCHAR(10) | VARCHAR(20) | IN_PROGRESS=11字符 |
| `tms03_work_process` | status | VARCHAR(10) | VARCHAR(20) | IN_PROGRESS=11字符 |
| `twh13_in` | refbillid | VARCHAR(8) | VARCHAR(30) | 工单号14字符 |
| `twh15_out` | refbillid | VARCHAR(8) | VARCHAR(30) | 工单号14字符 |
| `twh12_detaildt` | refbillid | VARCHAR(8) | VARCHAR(30) | 工单号14字符 |
| `tqc10_result` | refbillid | VARCHAR(8) | VARCHAR(30) | 入库单号9字符 |
| `tqc10_result` | qcbillid | VARCHAR(8) | VARCHAR(12) | QC+YYMMDD+3位=11字符 |
| `tqc10_result` | memo | — | VARCHAR(200) | 新增备注字段 |
| `tqc11_resultdt` | qcbillid | VARCHAR(8) | VARCHAR(12) | 同上 |
| `tqc11_resulteid` | qcbillid | VARCHAR(8) | VARCHAR(12) | 同上 |
| `tpc03_pcplanstatus` | refbillid | VARCHAR(8) | VARCHAR(30) | 统一关联单号长度 |
| `twh16_outdtprd` | ref_inbillid | — | VARCHAR(8) | 新增：来源入库单号（质检出库追溯） |
| `twh16_outdteid` | ref_inbillid | — | VARCHAR(8) | 新增：来源入库单号（质检出库追溯） |

### 模型/Schema 同步

| 文件 | 变更 |
|------|------|
| `app/models/mes.py` | WorkOrder.status, WorkProcess.status → String(20) |
| `app/models/warehouse.py` | StockIn/StockOut/QcResult.refbillid → String(30); QcResult/QcResultDt/QcResultEid.qcbillid → String(12); QcResult +memo |
| `app/schemas/warehouse.py` | StockInCreate/StockOutCreate.refbillid max_length=30 |
| `app/schemas/qc.py` | QcCreate.refbillid max_length=30; +optyp 字段 |
| `app/repositories/qc_repository.py` | _gen_qc_id 生成规则 QC+YYMMDD+3位序号（配合 varchar(12)） |

---

## 七、相关文档

| 文档 | 定位 |
|------|------|
| `数据库字典_PostgreSQL当前版.md` | **权威**：当前库完整字段字典（143 表，含 TPC14_DT） |
| `数据库ER关系文档.md` | **权威**：业务模型分类 + ER 关联 + 统计汇总（144 模型） |
| `数据库字典_精简后_最终版.md` | 历史：Oracle 迁移基线（130 表，Oracle 类型） |
| `数据迁移执行方案_v2.md` | 历史：迁移过程和分类评估 |
| `P0数据治理记录.md` | P0 数据修复操作详情 + 自动纠正机制 |

---

## 八、2026-07-15 派工与通知优化第一阶段

### 8.1 字段新增

| 表 | 字段 | 类型 | 说明 |
|----|------|------|------|
| `tmc12_groups` | `leader_cd` | VARCHAR(6) | 组长用户编码（TT=1612 张剑，A1 待定） |

### 8.2 新增表

| 表 | 说明 |
|----|------|
| `tit30_dispatch_rule` | 派单规则引擎：故障类型→派单目标→两级兜底链，预置 4 条规则 |

### 8.3 第二阶段补充（2026-07-15）

- 组管理页加组长字段：`tmc12_groups.leader_cd` 已在 `GroupList.vue` 编辑对话框支持，TT 组长已设 1612
- 派单规则配置页：`DispatchRuleList.vue` + GET/POST/PUT/DELETE `/dispatch-rules`
- 派工 Tab 通知编辑入口："编辑通知"/"生成通知"按钮 → 跳转 `NotificationList`
- 维护单列表"派给我"/"本区域"Tab：`list_by_filters` 加 `dispatch_to`/`area_user` 参数

### 8.4 第三阶段（2026-07-15）

| 变更 | 说明 |
|------|------|
| `tntf02_notification.retry_count` | INTEGER，通知发送失败重试次数（累加计数） |
| `DispatchRuleService.resolve` | 评分机制：fault_type+store_id 精确匹配优先，加 `load_balance` 目标（组内最少未关单优先） |
| `NotificationTemplateService.preview` | Jinja2 渲染预览，实时 400ms 防抖 |
| `NotificationList.vue` 重写 | 过滤栏 + 发送/重试按钮 + query 过滤 |
| `NotificationTemplateList.vue` 重写 | 编辑器 + 变量按钮 + 预览面板 |

---

## 十、2026-07-27~29 1a阶段：设备资产联动+业务规则

### 10.1 字段新增

| 表 | 字段 | 类型 | 说明 |
|----|------|------|------|
| `plan_cust` | `business_mode` | VARCHAR(2) | 业务模式（BM_S字典：01销售/02租赁/03借用/04代维/05寄售/06试用/07投放/08合作） |
| `tmm35_cust_pos_rl` | `business_mode` | VARCHAR(2) | 同上，设备级 |
| `tmm43_eid` | `warranty_expire` | TIMESTAMP | 保修到期日（L2关单写入：install_date + old_degree×newperiod/oldperiod） |
| `tit21_maintenance_dispatch` | `service_responsibility` | VARCHAR(2) | 服务责任方（SR字典：01内部/02厂商/03代维） |

### 10.2 字典新增

| code_typ | code_cd | 说明 |
|----------|---------|------|
| BM_S | 01-08 | 业务模式 8 档 |
| SR | 01-03 | 服务责任方 3 档 |
| SLA | close_days, scan_interval | SLA 超时自动关单配置 |

### 10.3 数据修正

| 修正 | 说明 |
|------|------|
| `tmm43_eid.install_date` | 用 `tmm35_cust_pos_rl.posupddate` 回填（4,020条），无POS记录的用 gendate（4,882条） |
| `tmm43_eid.warranty_expire` | 重算：修正后的 install_date + old_degree×newperiod/oldperiod（8,902条） |
| `asset_owner` | ZC→OW 迁移（2026-07-26），OW 01=商用电子(租赁)/03=门店资产(购买)/04=海晟 |
| `ZC 字典` | 废弃（useflg='0'），零代码引用 |

### 10.4 字段说明更正

| 表 | 字段 | 旧说明 | 新说明 |
|----|------|--------|--------|
| `tmm43_eid` | `new_old` | 新旧标志 | 【已废弃】已由 asset_type 替代 |
| `tmm43_eid` | `old_degree` | 旧化程度 | 质保范围（OD字典：12=新品质保/3=旧品质保），L2关单写入 |
| `tmm43_eid` | `asset_type` | 资产类型 | 替代 new_old：新采购→01新机/返修→02旧机/翻新→03翻新机/报废→04 |

---

## 九、2026-07-20~22 上门服务与配件收费优化（A→D 阶段）

### 9.1 A 阶段：上门服务补全

| 表 | 字段 | 类型 | 说明 |
|----|------|------|------|
| `tit23_maintenance_d2d` | `gzdm` | VARCHAR(8) | 故障代码（方案B，落库支持单次上门追溯） |
| `tit23_maintenance_d2d` | `device_id` | VARCHAR(13) | 本次处理设备（重构新增） |
| `tit23_maintenance_d2d` | `accessories_id` | VARCHAR(13) | 本次处理配件（重构新增） |
| `tit23_maintenance_d2d` | `d2d_phenomenon` | VARCHAR(200) | 四要素：实际现象 |
| `tit23_maintenance_d2d` | `d2d_reason` | VARCHAR(200) | 四要素：原因 |
| `tit23_maintenance_d2d` | `d2d_handling` | VARCHAR(500) | 四要素：处理过程 |
| `tit23_maintenance_d2d` | `d2d_result` | VARCHAR(2) | 四要素：结果（ZT字典：5/4/6/7/3） |
| `tit23_maintenance_d2d` | `closure_reason` | VARCHAR(1) | 四要素：关闭原因（CLO_REASON字典） |
| `tit23_maintenance_d2d` | `d2d_note` | VARCHAR(200) | 四要素：其他补充 |

### 9.2 B 阶段：故障代码优化

| 表 | 字段 | 类型 | 说明 |
|----|------|------|------|
| `tit25_accessories_update` | `itemcd` | VARCHAR(13) | 配件物料编码（B3：关联故障代码用） |
| `tit25_accessories_update` | `fault_cd` | VARCHAR(8) | 故障代码（B3：配件更换时自动关联） |

### 9.3 C 阶段：配件更换与收费合并

| 表 | 字段 | 类型 | 说明 |
|----|------|------|------|
| `tit25_accessories_update` | `payje` | NUMERIC(10,3) | 收款金额（吸收 TIT26） |
| `tit25_accessories_update` | `paytype` | VARCHAR(20) | 收费类型（C_TYPE字典） |
| `tit25_accessories_update` | `paydate` | TIMESTAMP | 收款日期（吸收 TIT26） |
| `tit25_accessories_update` | `memo` | VARCHAR(200) | 备注（吸收 TIT26） |
| `tit25_accessories_update` | `version` | INTEGER | 乐观锁版本号 |

**字典新增**：C_TYPE(5值)、CLO_REASON(5值)、PAY_SVC(4值)、PAY_CONS(7值)

**C3 数据迁移**：TIT26 → TIT25（112行：c_type=3 纯服务费109行 + c_type=5 耗材3行）

### 9.4 D 阶段：统计视图

| 对象 | 类型 | 说明 |
|------|------|------|
| `v_fault_analysis` | VIEW | 故障分析视图，工单维度聚合（10.9万行） |

**视图演进**：
- D1: 初始 D2D 行级视图
- D2: 改工单级聚合 + faultcode 解析回填（63,708 工单恢复 gzdm）
- D3: 增加优化列（d2d_device_id/d2d_accessories_id/accessory_itemcd/accessory_fault_cd）

### 9.5 种子数据

| 表 | 方式 | 说明 |
|----|------|------|
| `tit04_archivecode` | CSV seed | 609 条故障代码 |
| `tmc01_menus` | CSV seed | 23 条菜单 |
| `tmc02_menusdt` | CSV seed | 163 条菜单权限 |

命令：`flask seed check` / `flask seed load`
