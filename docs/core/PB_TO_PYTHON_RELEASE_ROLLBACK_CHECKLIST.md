# PB→Python 发布与回滚检查清单

## 1. 发布前检查

### 1.1 基线与映射
- [ ] 本次发布范围已按 `.pbl` 模块边界明确。
- [ ] `PB_TO_PYTHON_MODULE_MAPPING.csv` 已更新到最新。
- [ ] `PB_TO_PYTHON_SQL_MAPPING.csv` 已更新到最新。
- [ ] 与 `数据库字典_精简后_最终版.md` 口径一致。

### 1.2 质量与安全
- [ ] CI 门禁全绿（black/isort/ruff/mypy/pytest/bandit）。
- [ ] 关键日志字段已脱敏。
- [ ] 配置与密钥均来自环境变量/密钥管理器。

### 1.3 运行与观测
- [ ] 健康检查通过。
- [ ] 指标采集与告警规则已启用。
- [ ] 请求链路可追踪（`request_id/trace_id`）。

## 2. 灰度发布执行
- [ ] 批次 1（小流量）发布完成并观察。
- [ ] 批次 2（中流量）发布完成并观察。
- [ ] 批次 3（全量）发布完成并观察。
- [ ] 每批次均记录错误率、耗时、核心业务成功率。

## 3. 回滚触发条件（任一满足即触发）
- [ ] 核心链路错误率超过阈值。
- [ ] 关键接口 P95 超时连续超阈值。
- [ ] 数据一致性校验失败。
- [ ] 业务方确认出现语义偏差。

## 4. 回滚执行步骤
1. 停止继续放量并冻结变更。
2. 按模块级回滚预案回切应用版本。
3. 执行数据回滚/补偿脚本（如适用）。
4. 复核核心接口与关键 SQL 一致性。
5. 通知业务与项目组回滚完成状态。

## 5. 回滚后复盘
- [ ] 形成问题根因分析（RCA）。
- [ ] 明确修复方案与责任人。
- [ ] 更新映射清单与验收标准。
- [ ] 复盘结论回写到项目进度文档。

## 6. 记录区
- 发布版本：
- 发布时间：
- 回滚时间（如发生）：
- 指挥人：
- 记录人：
- 结论：
