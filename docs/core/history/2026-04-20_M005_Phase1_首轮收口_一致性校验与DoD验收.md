# M005 Phase 1 首轮收口执行明细（2026-04-20）

## 文档信息
- 归档时间：2026-04-20
- 关键步骤主题：`M005 (itsm02.pbl)` 首轮迁移收口，一致性校验矩阵与 DoD 验收记录沉淀
- 对应主文档：`docs/core/项目整体计划与进展_2026-03-11.md`

## 范围定义

### PB 模块范围
- 模块：`itsm02.pbl`（M005）
- 对象范围：`u_itsm_store_close`、`u_itsm_free_replace`、`u_itsm_liability`、`u_ex_gh`、`u_itsm_maintenance_plan`

### SQL 映射范围
- 本次收口对应 `S047~S065`（见 `docs/core/PB_TO_PYTHON_SQL_MAPPING.csv`）

## 已落地产物清单
- `app/repositories/store_close_repository.py`
- `app/services/store_close_service.py`
- `app/api/store_close.py`
- `app/repositories/free_replace_repository.py`
- `app/services/free_replace_service.py`
- `app/api/free_replace.py`
- `app/repositories/liability_repository.py`
- `app/services/liability_service.py`
- `app/api/liability.py`
- `app/repositories/ex_gh_repository.py`
- `app/services/ex_gh_service.py`
- `app/api/ex_gh.py`
- `app/repositories/maintenance_plan_repository.py`
- `app/services/maintenance_plan_service.py`
- `app/api/maintenance_plan.py`
- `app/__init__.py`（蓝图注册）

## SQL 一致性校验矩阵（S047~S065）

| 映射段 | 业务对象 | 关键表 | 结论 | 证据 |
|---|---|---|---|---|
| S047 | store_close 列表/详情 | TIT18_STORE_CLOSE | 已完成 PB->Python 映射与接口落地 | `PB_TO_PYTHON_SQL_MAPPING.csv`、`app/repositories/store_close_repository.py` |
| S048~S050 | free_replace 主子表+作废关单 | TIT28_FREE_REPLACE/TIT28_FREE_REPLACE_DT/TIT24/TIT27 | 已对齐 PB 约束（提交态/回访/关单写入） | `app/services/free_replace_service.py` |
| S051~S054 | liability 列表保存审核字典 | TIT10_MAINTENANCE_LIABILITY 等 | 已对齐 PB 规则（`exemptflg=Y` 必填，`is_finish` 状态推进） | `app/services/liability_service.py` |
| S055~S059 | ex_gh 列表新增送审作废 | TSL01_EXTEND/TSL02_EXTENDDT/TSL10_SLBILL | 已对齐 PB 规则（送审 0->1、作废回写 planqty） | `app/services/ex_gh_service.py` |
| S060~S065 | maintenance_plan 查询增删改生成年 | TIT17_MAINTENANCE_PLAN/TMM46_AREA | 已对齐 PB 规则（历史年月不可增删改、按年逐月生成） | `app/services/maintenance_plan_service.py` |

> 说明：以上为“映射与代码实现一致性”收口结论；数据库真值比对（结果集行级核验）将在联调环境按用例继续回填。

## DoD 验收记录（M005 Phase 1）

### 1) 功能等价
- [x] 模块内功能点清单已梳理并落在 `M005_itsm02_模块对象映射清单.md`
- [x] 功能点映射关系已回写 `PB_TO_PYTHON_MODULE_MAPPING.csv`
- [x] 核心业务路径已按 PB 规则完成服务层对齐（规则口径已沉淀于第7章）

### 2) SQL 与数据一致性
- [x] 关键 SQL 映射已覆盖 `S047~S065`
- [x] 每条关键 SQL 已形成映射口径与校验入口
- [x] 表结构与字段语义已按 `数据库字典_精简后_最终版.md` 对齐

### 3) 架构与代码规范
- [x] 落在 `api/services/repositories` 分层
- [x] `api` 层无 SQL，业务规则集中在 `service`
- [x] 新增/修改核心公共方法已补类型注解
- [x] 统一使用 `logging`，无 `print`

### 4) 质量门禁
- [ ] `black`（已执行命令，当前环境缺少 black 模块）
- [ ] `isort`（已执行命令，当前环境缺少 isort 模块）
- [x] `ruff`（已修复 `store_close` 相关 `F401/F541` 后通过）
- [ ] `mypy --strict`（已执行命令，当前环境缺少 mypy 模块）
- [ ] `pytest`（已执行，当前仓库未收集到 M005 相关测试用例）
- [ ] `bandit`（已执行命令，当前环境缺少 bandit 模块）
- [x] `compileall`（针对本轮新增与注册文件已通过）

### 5) 接口与契约
- [x] API 路径统一 `/api/v1`
- [ ] 响应结构 `{ code, message, data }` 全局统一（历史模块仍有 `{ success, data, error }` 口径，需后续统一）
- [ ] 请求/响应 schema 严格校验（当前以手工参数校验为主，后续需补齐）
- [x] 错误响应包含 `request_id`

### 6) 发布与回滚准备
- [ ] 灰度范围与放量节奏（待补）
- [ ] 回滚步骤确认与演练（待补）
- [ ] 发布窗口联系人与值班（待补）

## 验收结论
- 结论：**有条件通过（开发实现与映射收口完成）**。
- 已完成：M005 Phase 1 对象首轮迁移、映射回写、规则对齐、历史明细归档。
- 待完成：联调环境 SQL 结果一致性验证、补齐门禁工具依赖后重跑（black/isort/mypy/bandit）及 M005 相关测试用例、契约统一化与发布回滚演练。

## 下一步（尽快落地顺序）
1. 按 `S047~S065` 在联调库执行结果一致性 SQL，比对 PB 口径并回填“通过/差异”。
2. 补执行 `black/isort/mypy --strict/pytest/bandit`，形成门禁截图或日志证据。
3. 输出 `M005` 灰度发布与回滚清单，回写主文档第 7 章与第 8 章摘要。
