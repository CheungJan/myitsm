# P5 重启校准执行明细 - M005 Phase 1（2026-04-14）

## 文档信息
- 归档时间：2026-04-14
- 关键步骤主题：P5 重启后对 M005 Phase 1 执行基线进行校准
- 对应主文档：`docs/core/项目整体计划与进展_2026-03-11.md`

## 执行背景

本次操作遵循 `AGENTS.md` 与主计划文档的重构开工要求，先核对规则、再读取 PB 源码、再确认当前 Python 落地边界，避免在模块真实状态不清晰的情况下直接继续编码。

## 本次校准读取范围

### 已读取规则与计划文档
- `AGENTS.md`
- `docs/core/项目整体计划与进展_2026-03-11.md`
- `docs/core/PB_TO_PYTHON_OPTIMIZATION_REQUIREMENTS.md`
- `docs/core/PB_TO_PYTHON_MODULE_MAPPING.csv`
- `docs/core/PB_TO_PYTHON_SQL_MAPPING.csv`

### 已核对的 M005 相关文档
- `docs/core/M005_itsm02_模块对象映射清单.md`
- `docs/core/PB源码SQL提取分析_2026-04-14.md`
- `docs/core/history/2026-04-14_P5启动_M005_itsm02_模块映射建立.md`
- `docs/core/history/2026-04-14_P5重启_源码验证_执行明细.md`

### 已核对的代码与 PB 源对象
- `app/repositories/store_close_repository.py`
- `app/services/store_close_service.py`
- `app/api/store_close.py`
- `app/__init__.py`
- `itsm02.pbl/u_itsm_free_replace.sru`（通过 grep 抽取关键语句）
- `itsm02.pbl/w_r_itsm_replace.srw`（通过 grep 抽取关键语句）

## 校准结论

### 1. P5 当前状态不是“待启动”，而是“Phase 1 进行中”
- `TIT18_STORE_CLOSE` 已完成 repository/service/api 三层实现。
- `store_close_bp` 已在 `app/__init__.py` 注册。
- 因此 M005 当前状态应从“启动中”校准为“进行中”。

### 2. M005 Phase 1 下一目标应明确为免费更换主子表
- 下一实现对象：`TIT28_FREE_REPLACE`
- 下一实现对象：`TIT28_FREE_REPLACE_DT`
- 当前项目中尚无 `free_replace` 相关 Python 实现文件。

### 3. 免费更换单的 PB 关键约束已确认
- 作废前检查：`TIT28_FREE_REPLACE_DT` 中若存在 `IS_FINISH='1'` 记录，则不可作废。
- 关单前检查：`tit24_maintenance_rv` 中必须存在当前单据的回访记录。
- 关单前检查：`TIT28_FREE_REPLACE_DT` 中若存在 `IS_FINISH='0'` 记录，则不可关闭。
- 关闭动作：关闭时写入 `TIT27_CLOSE_BILLS`。

## 对后续实现的约束

### 设计约束
- 继续遵循 Flask 应用工厂与 `api/services/repositories` 三层分层。
- `api` 层仅做参数校验、协议转换、统一响应组装。
- 事务边界放在 `service` 层，不在 `api` 层或 `repository` 层分散处理业务事务。

### 业务约束
- 免费更换单实现必须同时考虑主表与子表，不能只落主表而忽略设备明细校验。
- 关闭流程不能绕过回访记录与未提交明细校验。
- 关单能力若落地，应同步考虑 `TIT27_CLOSE_BILLS` 的公用表复用边界。

## 本次同步更新
- 更新 `docs/core/项目整体计划与进展_2026-03-11.md` 的 P5 摘要口径。
- 更新 `docs/core/PB_TO_PYTHON_MODULE_MAPPING.csv` 的 M005 状态与备注。
- 更新 `docs/core/HISTORY_INDEX.md` 增加本归档索引。

## 下一步建议
1. 先在 `docs/core/PB_TO_PYTHON_SQL_MAPPING.csv` 为 `TIT28_FREE_REPLACE / TIT28_FREE_REPLACE_DT` 建立正式 SQL 映射项。
2. 再按主表、子表、关单约束三个层次实现 `free_replace` 的 repository/service/api。
3. 实现完成后同步回写主文档摘要与 history 明细。
